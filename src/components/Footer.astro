---
interface Props {
	dark?: boolean;
	locationText?: string;
}

const { dark = false, locationText = 'Living, playing and creating in Madrid, Spain' } = Astro.props;
const bgColor = dark ? 'bg-secondary' : 'bg-primary';
const borderColor = dark ? 'border-primary' : 'border-secondary';
const textColor = dark ? 'text-primary' : 'text-secondary';
const textSecondaryColor = dark ? 'text-secondary-200' : 'text-secondary-300';
---

<footer class={`${bgColor} border-t-2 ${borderColor} min-h-[329px] px-16 py-12 flex items-center justify-between`}>
	<div class="flex flex-col gap-2">
		<p class={`${textColor} font-sans text-body-md`}>{locationText}</p>
		<p class={`${textSecondaryColor} font-sans text-body-sm`}>Product Designer</p>
		<p class={`${textSecondaryColor} font-sans text-body-sm`}>Miriam Laina ©</p>
	</div>
	<div class="flex items-center justify-center">
		<a href="https://www.linkedin.com/in/miriam-laina-p%C3%A9rez-622956168/" target="_blank" class={`${textColor} hover:opacity-70 transition-opacity`}>
			<svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
				<path d="M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.761 2.239 5 5 5h14c2.762 0 5-2.239 5-5v-14c0-2.761-2.238-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.764s.784-1.764 1.75-1.764 1.75.79 1.75 1.764-.783 1.764-1.75 1.764zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.777 7 2.476v6.759z"/>
			</svg>
		</a>
	</div>
	<div class="flex items-center justify-end">
		<span class={`${textColor} font-sans font-bold text-xl rotate-180 inline-block`}>iC</span>
	</div>
</footer>

