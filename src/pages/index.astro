---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/news/Header.astro';
import NewsHeader from '../components/news/Header.astro';
import NewsButton from '../components/news/Button.astro';
import DataItem from '../components/news/DataItem.astro';
import Footer from '../components/news/Footer.astro';
import ProjectLarge from '../components/news/ProjectLarge.astro';
import SideProjectItem from '../components/news/SideProjectItem.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
import { projects } from '../data/projects';

const mainProjects = projects.filter(p => !p.isSideProject);
const sideProjects = projects.filter(p => p.isSideProject);
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body class="bg-primary min-h-screen flex flex-col">
		<!-- Noise overlay que está por encima de todo -->
		<!-- <div class="noise-overlay"></div> -->
		<Header theme="light" />
		
		<main class="w-full flex flex-col">
			<!-- Hero Section -->
			<section class="w-full px-16 py-20 flex flex-col md:flex-row items-center justify-between gap-12 min-h-[758px]">
				<div class="flex flex-col gap-[56px] max-w-[764px]">
					<div class="flex flex-col gap-0">
						<h2 class="text-h3 font-sans text-secondary">hey,</h2>
						<h1 class="text-h1 font-heading font-bold text-secondary">welcome!</h1>
					</div>
					<div class="flex flex-col gap-4 text-body-md font-body text-secondary-300">
						<p>I am Miriam. I believe great design begins with humility and curiosity understanding the problem deeply before crafting the solution. My work fuses creativity, strategy, and technology to craft products that not only stand out but truly improve people's lives.</p>
						<p class="font-bold">This is a curated peek into my work and how I think.</p>
					</div>
				</div>
				<div class="w-[420px] h-[420px] flex-shrink-0">
					<img src="/a.svg" alt="Image Grid" class="w-full h-full object-cover" />
				</div>
			</section>

			<!-- Image Grid Section -->
			<section class="w-full border-t-2 border-b-2 border-secondary h-[284px] flex items-center overflow-hidden">
				<div class="flex items-center h-full">
					<div class="w-[380px] h-full flex-shrink-0 border-r-2 border-secondary bg-secondary-100"></div>
					<div class="w-[380px] h-full flex-shrink-0 border-r-2 border-secondary bg-secondary-100"></div>
					<div class="w-[380px] h-full flex-shrink-0 border-r-2 border-secondary bg-secondary-100"></div>
					<div class="w-[380px] h-full flex-shrink-0 bg-secondary-100"></div>
				</div>
			</section>

			<!-- Philosophy Section -->
			<section class="w-full px-16 py-[120px] flex flex-col items-center gap-14">
				<h2 class="text-h3 font-heading font-bold text-secondary text-center">PHILOSOPHY BEHIND THE PIXELS</h2>
				<div class="flex flex-col gap-4 max-w-[1152px] text-body-md font-body text-secondary-300 text-center">
					<p>I start from the real problem before falling in love with the visual solution, because design is never just about aesthetics.
						<br/> It's about solving problems grounded in data, so others don't have to. It's about simplifying the complex and turning that simplicity into a guiding principle.
						<br/> It's about creating experiences that stand out and stick with people, while feeling so seamless they're simply enjoyable to use.
					</p>
					<p class="font-bold font-body">Above all, it's about harnessing technology to make life better.</p>
				</div>
				<NewsButton href="/about" text="Read about me" />
			</section>

			<!-- Banner Section -->
			<section class="w-full bg-tertiary-300 px-4 py-10 flex items-center justify-center gap-6">
				<h2 class="text-h2 font-heading font-bold text-primary text-center whitespace-nowrap">CREATE DESIGN FROM HUMAN FOR HUMANS</h2>
			</section>

			<!-- Projects Section -->
			<section class="w-full pl-16 py-[120px] flex flex-col gap-12">
				<h2 class="text-h3 font-heading font-bold text-secondary">Projects</h2>
				
				<div class="flex flex-col w-full border-b-2 border-secondary">
					{mainProjects.map((project, index) => (
						<div class="border-t-2 border-secondary">
							<ProjectLarge 
								imageSrc={project.heroImage || '/placeholder-hero.jpg'}
								category={project.industry || ''}
								title={project.title}
								description={project.description.split('\n')[0]}
								tags={project.tags || []}
								slug={project.slug}
							/>
						</div>
					))}
				</div>
			</section>

			<!-- Industries Section -->
			<section class="w-full bg-tertiary-300 px-14 py-[120px] flex flex-col items-center gap-16">
				<h2 class="text-h2 font-heading font-bold text-tertiary-100 text-center">INDUSTRIES OF EXPERTISE</h2>
				<div class="flex gap-6 flex-wrap justify-center">
					<DataItem variant="icon" cardStyle="filled" title="Security" />
					<DataItem variant="icon" cardStyle="filled" title="Energy" />
					<DataItem variant="icon" cardStyle="filled" title="Telco" />
					<DataItem variant="icon" cardStyle="filled" title="Automotive" />
					<DataItem variant="icon" cardStyle="filled" title="AI" />
				</div>
			</section>

			<!-- Side Projects Section -->
			<section class="w-full bg-secondary px-16 py-[120px] flex flex-col md:flex-row gap-14 items-end">
				<div class="flex flex-col gap-6 max-w-[548px]">
					<h2 class="text-h2 font-heading font-bold text-primary">
						SIDE<br/>PROJECTS
					</h2>
					<p class="text-body-md font-body text-secondary-200 pb-4">Personal projects where I explore ideas, technologies, and narratives that inspire me. Spaces for experimentation — to learn, fail, and create without limits.</p>
				</div>
				<div class="flex-1 flex flex-col gap-0">
					{sideProjects.map((project, index) => (
						<SideProjectItem 
							title={project.title}
							description={project.description.split('\n')[0]}
							slug={project.slug}
							isLast={index === sideProjects.length - 1}
						/>
					))}
				</div>
			</section>
		</main>

		<Footer />
	</body>
</html>
